
// header toggle codes starts
headerToggle.addEventListener('click', ()=>{
    if(header.style.display === 'flex'){
        header.style.display = 'none'
    } else{
        header.style.display = 'flex'
    }
})
// header toggle codes ends